class game
  constructor: (canvas_id) ->
    @canvas = document.getElementsByTagName(canvas_id)[0]
    @width = canvas.getAttribute('width')
    @height = getAttribute('height')
    @canvas.fillStyle = '#000';
    console.log @width + ' ' + @height
  
  update: ->

  draw: ->
